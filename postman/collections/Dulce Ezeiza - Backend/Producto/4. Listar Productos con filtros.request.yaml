$kind: http-request
url: "{{baseUrl}}/productos?soloActivos=true&sinGluten=false"
method: GET
queryParams:
  soloActivos: "true"
  sinGluten: "false"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 200', function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 4000
