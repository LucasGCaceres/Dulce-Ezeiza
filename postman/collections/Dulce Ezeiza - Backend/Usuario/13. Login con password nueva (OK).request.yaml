$kind: http-request
description: "Confirma que el cambio de password quedo guardado de verdad. De paso renueva {{tokenCliente}} para las carpetas que siguen."
url: "{{baseUrl}}/usuarios/login"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
        "email": "{{clienteEmail}}",
        "password": "{{clientePasswordNueva}}"
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 200', function () {
          pm.response.to.have.status(200);
      });
      const jsonData = pm.response.json();
      if (jsonData.token) {
          pm.collectionVariables.set('tokenCliente', jsonData.token);
      }
    language: text/javascript
order: 13000
